name 'peopleware-enable_puppet'
version '0.0.1'
summary "Install puppet and start agent on boot"
description "Install puppet and start the agent on boot by setting /etc/default/puppet to START=yes."
license "Apache 2.0"
project_page "https://github.com/peopleware/puppet-enable_puppet"
